{
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && npx tsc --project tsconfig.seed.json && node dist/prisma/seed.js",
  "rewrites": [
    { "source": "/:path*", "destination": "/api/index" }
  ]
}
